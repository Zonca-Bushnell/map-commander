# Optional Python scripts

This folder is reserved for optional helper scripts.

If a Python helper is added, run it through the project virtual environment:

```bat
.venv\Scripts\python.exe scripts\your_script.py
```

The editor itself does not require Python at runtime.
